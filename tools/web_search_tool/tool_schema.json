{
  "type": "function",
  "function": {
    "name": "web_search",
    "description": "Search the web with DuckDuckGo HTML search and return organic results. No API key or self-hosted search instance is required.",
    "parameters": {
      "type": "object",
      "properties": {
        "query": {
          "type": "string",
          "description": "The search query. Pass the planner's query exactly; the tool does not rewrite or expand it."
        },
        "num_results": {
          "type": "integer",
          "description": "Maximum number of results to return.",
          "default": 5
        }
      },
      "required": ["query"]
    }
  }
}
