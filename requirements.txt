
openai==2.35.1
httpx>=0.27,<1
beautifulsoup4==4.14.3
crawl4ai>=0.7,<0.9
chromadb==1.5.9
ddgs==9.14.2
langchain-community==0.4.1
Pillow
pywin32; platform_system == "Windows"
uiautomation; platform_system == "Windows"
winsdk==1.0.0b10; platform_system == "Windows"
PySide6>=6.7,<7
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
python-multipart>=0.0.9
pypdf>=4.2.0
python-docx>=1.1.2
python-pptx>=1.0.2
# local corpus .xlsx parsing (services/local_corpus/parsers.py) + table query
# (services/local_corpus/table_query_service.py)
openpyxl>=3.1
olefile>=0.47
httpx>=0.27,<1
markdown>=3.6

# verification layer (services/verification/) — embedding + IR/NLP, no extra LLM calls
numpy>=1.26
networkx>=3.2
rank-bm25>=0.2.2
scikit-learn>=1.4
kiwipiepy>=0.17

# document export (api/services/export_service.py)
# pandoc is a system binary, not importable Python. `pypandoc-binary` bundles the
# pandoc executable so `pip install -r requirements.txt` enables DOCX / HTML / PDF
# export out of the box (Markdown export needs nothing). export_service finds
# pandoc on PATH, in the active conda/venv, or via this package — so
# `conda install -c conda-forge pandoc` works equally well.
pypandoc-binary>=1.13
# PDF export additionally needs a PDF engine (none are on PyPI). Install one:
#   conda install -c conda-forge wkhtmltopdf      # lightest
#   # or a TeX distribution providing xelatex / pdflatex
kiwipiepy_model>=0.17
